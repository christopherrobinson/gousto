---
const navItems = [
  { href: '/faqs/', label: 'FAQs' },
  { href: '/cookies/', label: 'Cookies' },
  { href: '/privacy/', label: 'Privacy' },
];
---

<footer class="bg-secondary-950  mt-auto  py-4  text-center  text-sm  text-white/80  print:hidden" transition:persist>
  <Container class="flex  flex-col-reverse  gap-2  items-center  justify-between  xs:flex-row">
    <span class="block  mb-0">{site.name}</span>
    {navItems && (
      <ul class="flex  items-center  justify-center  gap-4">
        {navItems.map(({ href, label }) => (
          <li><a class="transition  hover:text-white  hover:underline" href={href}>{label}</a></li>
        ))}
      </ul>
    )}
  </Container>
</footer>
